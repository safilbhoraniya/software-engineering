CREATE TABLE product(
  PRO_ID INT PRIMARY KEY,
  PRO_NAME VARCHAR(50),
  PRO_PRICE FLOAT,
  PRO_COMPANYCODE INT
);

INSERT INTO product
(PRO_ID,PRO_NAME,PRO_PRICE,PRO_COMPANYCODE)
VALUES

(101,"mother board",3200.00,15),
(102,"key board",450.00,16),
(103,"zip drive",250.00,14),
(104,"speaker",550.00,16),
(105,"monitor",5000.00,11),
(106,"DVD drive",900.00,12),
(107,"CD drive",800.00,12),
(108,"printer",2600.00,13),
(109,"refill cartridge",350.00,13),
(110,"mouse",250.00,12);

SELECT *FROM product;
SELECT *FROM product WHERE PRO_PRICE<=250;
SELECT *
FROM product
ORDER BY PRO_PRICE DESC;


SELECT *
FROM product
ORDER BY PRO_NAME ASC;

SELECT AVG(PRO_PRICE)
FROM product;

SELECT PRO_PRICE
FROM product;

SELECT PRO_COMPANYCODE
FROM product;

SELECT AVG(PRO_ID)
FROM product;
SELECT AVG(PRO_NAME)
FROM product;
SELECT AVG(PRO_COMPANYCODE)
FROM product;